name: Persona Security CI (Adversarial & Crypto Gate)

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  schedule:
    - cron: '0 2 * * *' # Nightly run at 2 AM UTC

jobs:
  security-verification:
    name: Cryptographic & Adversarial Security Gate
    runs-on: ubuntu-latest
    timeout-minutes: 25

    steps:
      - name: Checkout Source Code
        # actions/checkout@v4.1.7 (pinned to immutable commit SHA)
        uses: actions/checkout@692973e3d937129bcbf40652eb9f2f61becf3332

      - name: Set up JDK 17
        # actions/setup-java@v4.2.2 (pinned to immutable commit SHA)
        uses: actions/setup-java@99b8673ff64fbf99d8d325f52d9a5bdedb8483e9
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: 'gradle'

      - name: Static Security Scan (Hardcoded Secrets & Permissions)
        run: bash scripts/security-check.sh

      - name: Execute RFC 6238 TOTP Test Vectors
        run: ./gradlew testDebugUnitTest --tests "com.pims.vault.vault.TotpRfc6238Tests"

      - name: Execute Zone 4 15 Adversarial Attack Tests
        run: ./gradlew testDebugUnitTest --tests "com.pims.vault.vault.VaultAdversarialAttackTests"

      - name: Execute Sharing Protocol & Replay Tests
        run: ./gradlew testDebugUnitTest --tests "com.pims.vault.sharing.SharingProtocolTests"

      - name: Execute Cross-Zone Isolation Tests
        run: ./gradlew testDebugUnitTest --tests "com.pims.vault.integration.CrossZoneIsolationAttackTests"

      - name: Execute Full Lifecycle Journey Integration Tests
        run: ./gradlew testDebugUnitTest --tests "com.pims.vault.integration.FullLifecycleE2ETests"

      - name: Execute Disaster Recovery & Clock Manipulation Tests
        run: ./gradlew testDebugUnitTest --tests "com.pims.vault.integration.DisasterRecoveryAndClockTests"

      - name: Execute Room Schema Migration Tests
        run: ./gradlew testDebugUnitTest --tests "com.pims.vault.data.local.RoomMigrationTests"

      - name: Execute Milestone 8 Backup & Disaster Recovery Adversarial Tests
        run: ./gradlew testDebugUnitTest --tests "com.pims.vault.backup.BackupAdversarialTests"

      - name: Execute Milestone 8-RC Destructive Recovery & Crash Transition Tests
        run: ./gradlew testDebugUnitTest --tests "com.pims.vault.backup.M8RCDestructiveRecoveryTests"
